import { FC } from 'react';
import SidePanel from '@components/AppLayout/components/SidePanel/SidePanel.tsx';

export const AppLayout: FC = () => {
  return (
    <div>
      <SidePanel />
    </div>
  );
};

export default AppLayout;
